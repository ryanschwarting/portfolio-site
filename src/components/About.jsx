import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />
        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        I'm a full stack developer that possesses versatile skills in both
        front-end and backend development, employing technologies such as
        React/Next.js, TypeScript, JavaScript, Python, Node.js, Express, and
        Solidity. Specializing in front-end development, I excel in crafting
        dynamic interfaces using React with TypeScript, leveraging frameworks
        like Vite, Next, and Gatsby for high-performance and SEO-optimized web
        applications. Notably, skilled in creating responsive designs with
        Tailwind CSS and employ Framer Motion for crafting engaging UI
        animations. Also, demonstrate proficiency in decentralized application
        (dApp) development, particularly in Solidity smart contracts and
        utilizing JavaScript SDKs like web3.js and ethers.js. I exhibit
        expertise in cloud-based maintenance on platforms like AWS,
        DigitalOcean, and Vercel, ensuring rapid issue resolution and optimal
        user satisfaction. Additionally, adept at integrating AI and ChatGPT
        technologies for enhanced productivity.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
